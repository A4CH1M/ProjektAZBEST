<div class="filter" id="{{ filter_id }}">
    <label for="{{ id }}">{{ label }}</label>
    <input
            type="text"
            id="{{ id }}"
            name="{{ name }}"
            placeholder="{{ placeholder }}"
    >
</div>